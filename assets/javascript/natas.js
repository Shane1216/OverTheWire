$(document).ready(function(){
    $('#zero').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 0');
        let password = $('<p>').text('Password: natas0')
        let username = $('<p>').text('Username: natas0');
        let url = $('<p>').text('http://natas0.natas.labs.overthewire.org')

        $('#container').append(title, password, username, url);
    })
    $('#one').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 1');
        let username = $('<p>').text('Username: natas1');
        let url = $('<p>').text('http://natas1.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#two').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 2');
        let username = $('<p>').text('Username: natas2');
        let url = $('<p>').text('http://natas2.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#three').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 3');
        let username = $('<p>').text('Username: natas3');
        let url = $('<p>').text('http://natas3.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#four').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 4');
        let username = $('<p>').text('Username: natas4');
        let url = $('<p>').text('http://natas4.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#five').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 5');
        let username = $('<p>').text('Username: natas5');
        let url = $('<p>').text('http://natas5.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#six').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 6');
        let username = $('<p>').text('Username: natas6');
        let url = $('<p>').text('http://natas6.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#seven').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 7');
        let username = $('<p>').text('Username: natas7');
        let url = $('<p>').text('http://natas7.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#eight').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 8');
        let username = $('<p>').text('Username: natas8');
        let url = $('<p>').text('http://natas8.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#nine').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 9');
        let username = $('<p>').text('Username: natas9');
        let url = $('<p>').text('http://natas9.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#ten').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 10');
        let username = $('<p>').text('Username: natas10');
        let url = $('<p>').text('http://natas10.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#eleven').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 11');
        let username = $('<p>').text('Username: natas11');
        let url = $('<p>').text('http://natas11.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#twelve').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 12');
        let username = $('<p>').text('Username: natas12');
        let url = $('<p>').text('http://natas12.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#thirteen').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 13');
        let username = $('<p>').text('Username: natas13');
        let url = $('<p>').text('http://natas13.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#fourteen').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 14');
        let username = $('<p>').text('Username: natas14');
        let url = $('<p>').text('http://natas14.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#fifteen').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 15');
        let username = $('<p>').text('Username: natas15');
        let url = $('<p>').text('http://natas15.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#sixteen').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 16');
        let username = $('<p>').text('Username: natas16');
        let url = $('<p>').text('http://natas16.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#seventeen').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 17');
        let username = $('<p>').text('Username: natas17');
        let url = $('<p>').text('http://natas17.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#eighteen').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 18');
        let username = $('<p>').text('Username: natas18');
        let url = $('<p>').text('http://natas18.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#nineteen').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 19');
        let username = $('<p>').text('Username: natas19');
        let url = $('<p>').text('http://natas19.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#twenty').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 20');
        let username = $('<p>').text('Username: natas20');
        let url = $('<p>').text('http://natas20.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#twentyone').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 21');
        let username = $('<p>').text('Username: natas21');
        let url = $('<p>').text('http://natas21.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#twentytwo').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 22');
        let username = $('<p>').text('Username: natas22');
        let url = $('<p>').text('http://natas22.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#twentythree').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 23');
        let username = $('<p>').text('Username: natas23');
        let url = $('<p>').text('http://natas23.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#twentyfour').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 24');
        let username = $('<p>').text('Username: natas24');
        let url = $('<p>').text('http://natas24.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#twentyfive').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 25');
        let username = $('<p>').text('Username: natas25');
        let url = $('<p>').text('http://natas25.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#twentysix').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 26');
        let username = $('<p>').text('Username: natas26');
        let url = $('<p>').text('http://natas26.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#twentyseven').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 27');
        let username = $('<p>').text('Username: natas27');
        let url = $('<p>').text('http://natas27.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#twentyeight').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 28');
        let username = $('<p>').text('Username: natas28');
        let url = $('<p>').text('http://natas28.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#twentynine').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 29');
        let username = $('<p>').text('Username: natas29');
        let url = $('<p>').text('http://natas29.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#thirty').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 30');
        let username = $('<p>').text('Username: natas30');
        let url = $('<p>').text('http://natas30.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#thirtyone').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 31');
        let username = $('<p>').text('Username: natas31');
        let url = $('<p>').text('http://natas31.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#thirtytwo').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 32');
        let username = $('<p>').text('Username: natas32');
        let url = $('<p>').text('http://natas32.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#thirtythree').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 33');
        let username = $('<p>').text('Username: natas33');
        let url = $('<p>').text('http://natas33.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    $('#thirtyfour').on('click', function(event){
        event.preventDefault();

        clearFunction();
        let title = $('<h1>').text('Natas Level 34');
        let username = $('<p>').text('Username: natas34');
        let url = $('<p>').text('http://natas34.natas.labs.overthewire.org')

        $('#container').append(title, username, url);
    })
    
})




let clearFunction = function(){
    $('#container').empty();
  }